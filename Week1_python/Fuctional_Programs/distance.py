#import modules
import sys
import math

#define command line inputs
x = float(sys.argv[0])
y = float(sys.argv[1])

#calculate distance
distance = float(decimal(math.sqrt(x + y)))

#print result
print('Distance is ',distance)